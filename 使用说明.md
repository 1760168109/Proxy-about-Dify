# 岚本地代理

> 这份说明适合第一次接触命令行的人。照着做即可；如果某一步报错，把截图发给柳生。

## 一、这是什么

这是一个本地小代理。它会在你的电脑上启动一个地址：

~~~text
http://127.0.0.1:7272
~~~

Claude Code 会把请求发给这个地址，再由它转到 Dify 上的「岚」。

使用时请记住两点：

- 启动代理的黑色 / 蓝色窗口要一直开着，关掉后 Claude Code 就连不上。
- 柳生给你的 app-xxxx 是使用额度的钥匙，不要发给别人，也不要截图外传。

## 二、安装前准备

请先装好这三个东西：

1. PowerShell 7
   - 打开终端并输入 `pwsh`；能进入 PowerShell 7 就说明已经装好。
   - 如果提示找不到 `pwsh`，请先从 Microsoft Store 安装 PowerShell，安装后重新打开终端。

2. Python 3
   - 下载并安装 Python 3。
   - 安装第一页一定要勾选 Add python.exe to PATH 或 Add Python to PATH。
   - 如果忘了勾选，后面输入 python 会报找不到命令，需要重新安装或修复安装。

3. Claude Code
   - 按你平时使用 Claude Code 的方式安装。
   - 如果你使用 CC Switch，就按下面的 CC Switch 方式接入。(柳生建议新手用这个)
   - 如果不用 CC Switch，就按 settings.json 方式接入。

## 三、解压代理

如果柳生发给你的是 zip，按本节操作；如果柳生发给你的是 GitHub 链接，先看下一节“从 GitHub 下载”。

1. 找到柳生发来的 zip。
2. 右键解压到一个简单位置，例如：

~~~text
C:\lan-proxy
~~~

3. 解压后应该能看到这些文件（下一节从 GitHub 下载的情况，认的也是这一份清单）：

~~~text
install.ps1
lan.cmd
main.py
README.md
requirements.txt
.env.example
~~~

如果看到的是“文件夹里面又套了一层文件夹”，也没关系，后面进入真正包含 install.ps1 的那个文件夹。

## 四、从 GitHub 下载

如果柳生发给你的是 GitHub 链接，按这个方式下载：

1. 打开柳生发来的 GitHub 页面。
2. 找到绿色的 Code 按钮。
3. 点击 Code。
4. 点击 Download ZIP。
5. 下载完成后，右键解压到一个简单位置，例如：

~~~text
C:\lan-proxy
~~~

6. 打开解压后的文件夹，找到真正包含 install.ps1 的那一层（文件清单见上一节第 3 步）。

之后继续看下一节“第一次安装”。

## 五、第一次安装

1. 打开解压后的文件夹。
2. 在文件夹空白处按住 Shift，点击鼠标右键。
3. 选择“在终端中打开”或“在 PowerShell 中打开”。
4. 复制下面这行，粘贴进去，按回车：

~~~powershell
pwsh -ExecutionPolicy Bypass -File .\install.ps1
~~~

这里必须使用 `pwsh`（PowerShell 7），不要改成旧版的 `powershell`。如果提示找不到
`pwsh`，请先按“安装前准备”安装 PowerShell 7。

安装完成后，会看到类似：

~~~text
Installed.
1) Open a NEW PowerShell 7 window
2) Type: lan
3) Keep the window open while chatting
URL: http://127.0.0.1:7272
~~~

这一步只需要做一次。

## 六、填写自己的名字

第一次安装会自动生成一个 .env 文件。

1. 用记事本打开 .env。
2. 找到这一行：

~~~text
DIFY_USER_ID=Liu Sheng
~~~

3. 把 Liu Sheng 改成你自己的名字，例如：

~~~text
DIFY_USER_ID=你的名字
~~~

4. 保存文件。

说明：

- 这个名字用于区分会话和本地缓存。
- 不要和柳生或其他朋友用同一个名字。
- DIFY_API_KEY= 可以先空着；如果你用 CC Switch，密钥填在 CC Switch 里即可。

## 七、启动代理

1. 新开一个 PowerShell 7 或终端窗口。
2. 输入：

~~~powershell
lan
~~~

3. 看到服务启动后，不要关闭这个窗口。

如果想确认是否启动成功，可以在浏览器打开：

~~~text
http://127.0.0.1:7272/health
~~~

如果看到包含 ok、status 或类似状态信息，就说明代理正在运行。

## 八、接入 Claude Code

### 方式 A：使用 CC Switch

在 CC Switch 里填：

~~~text
Base URL: http://127.0.0.1:7272
API Key: 柳生单独发给你的 app-xxxx
格式: Anthropic Messages
Routing: 关闭
Model: alan
~~~

日常使用填 alan 即可；如果用“获取模型”，选择显示为“岚”的 `anthropic/alan`。

另外请顺手做一件事：打开 Claude Code 的 settings.json，在 env 里加上这两行（防止长任务
中途被判成“没反应”而断开，详见常见问题第 6 条）。

~~~json
"API_TIMEOUT_MS": "2333333",
"CLAUDE_STREAM_IDLE_TIMEOUT_MS": "2333333"
~~~

### 方式 B：不用 CC Switch

如果你不用 CC Switch，需要把 Claude Code 的环境变量设成：

~~~json
{
  "model": "alan",
  "env": {
    "ANTHROPIC_BASE_URL": "http://127.0.0.1:7272",
    "ANTHROPIC_AUTH_TOKEN": "柳生单独发给你的 app-xxxx",
    "ANTHROPIC_DEFAULT_FABLE_MODEL": "alan",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "alan",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "alan",
    "CLAUDE_CODE_ENABLE_GATEWAY_MODEL_DISCOVERY": "1",
    "API_TIMEOUT_MS": "2333333",
    "CLAUDE_STREAM_IDLE_TIMEOUT_MS": "2333333"
  }
}
~~~

最后那两行是超时上限（毫秒）。岚的复杂工作流有时要跑好几分钟，而 Claude Code 默认约
5 分钟收不到内容就会断开——不加这两行，长任务容易在中途断掉。

如果你不熟悉 settings.json，优先用 CC Switch，比较不容易填错。

## 九、日常使用流程

每次使用前：

1. 打开终端。
2. 输入：

~~~powershell
lan
~~~

3. 保持这个窗口开着。
4. 打开 Claude Code，选择或填写模型 alan。
5. 正常聊天或让它读写本地项目。

用完以后：

1. 先结束 Claude Code 的任务。
2. 再关闭运行 lan 的终端窗口。

## 十、常见问题

### 1. 输入 lan 后提示不是内部或外部命令

可能是安装后没有新开终端。

处理：

1. 关闭当前终端。
2. 新开一个 PowerShell 7。
3. 再输入 lan。

如果仍然不行，进入代理文件夹后运行：

~~~powershell
.\lan.cmd
~~~

### 2. 提示 python not found

说明 Python 没装好，或安装时没勾选 Add Python to PATH。

处理：

1. 重新安装 Python 3。
2. 安装第一页勾选 Add Python to PATH。
3. 安装后重新打开终端。

### 3. Claude Code 连接失败

按顺序检查：

1. lan 窗口是否还开着。
2. 浏览器能否打开 http://127.0.0.1:7272/health。
3. CC Switch 的 Base URL 是否是 http://127.0.0.1:7272。
4. API Key 是否是柳生给你的 app-xxxx。
5. Routing 是否关闭。
6. Model 是否为 alan，或“获取模型”列表中的 `anthropic/alan`。

### 4. 回答很奇怪或串到旧对话

检查 .env 里的：

~~~text
DIFY_USER_ID=你的名字
~~~

如果你和别人用了同一个名字，可能会造成会话或缓存混在一起。

### 5. 想把报错发给柳生

优先发这三样：

1. Claude Code 里的报错截图。
2. 运行 lan 的终端截图。
3. 浏览器打开下面地址后的截图：

~~~text
http://127.0.0.1:7272/debug/last-request
~~~

注意：这个调试页面可能包含你刚刚发送的内容或文件片段。若有隐私内容，先告诉柳生。

### 6. 长任务跑到一半就断了

现象：岚在处理一个大任务，Claude Code 那边等了几分钟后报错或没了反应，而 lan 的窗口
还在继续输出。

原因：Claude Code 默认约 5 分钟收不到内容就断开，而岚的复杂工作流可能更久。

处理：在 Claude Code 的 settings.json 的 env 里加上这两行（数字是毫秒，可按需调大）：

~~~json
"API_TIMEOUT_MS": "2333333",
"CLAUDE_STREAM_IDLE_TIMEOUT_MS": "2333333"
~~~

保存后当前会话即生效，不用重开 Claude Code。顺便说一句：即使 Claude Code 这边断了，
岚在 Dify 上仍会跑完，那一枪的额度已经花掉了——所以最好一开始就把这两行加上。
