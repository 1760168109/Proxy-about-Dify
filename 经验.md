# 经验

> 实战换来的守则。每条一律一因；违背任何一条之前，先读懂它为何存在。
> 只留当前仍有效的判断；安装与机制分别见 `README.md`、`架构.md`。

---

## 守则

1. **出站流式按 Anthropic 约定裁决，勿默认 SSE。** body.stream 优先，省略时看 Accept 头，两者皆无回 JSON；曾无条件默认 SSE，CC 按 JSON 解析导致主枪整屏空白。已固化在判枪逻辑，不设开关。
2. **文本标记通道不手写长 JSON 字符串。** 转义即脆弱：裸换行、未转义引号、`C:\U…` 非法转义都曾让整块工具调用作废、文件不落盘。长字段走 `[[raw:键]]` 原文块；结构化出口由 schema 约束合法 JSON。
3. **可执行块解析失败必须可见。** 坏工具块保留为正文、原文块开而未闭整块弃置——静默半调用（如 Write 丢 content）比失败更糟。
4. **主会话指针只随 opus 主窗口枪。** title/recap/compact 与子代理续主 conversation_id 会污染岚的会话变量；隔离的是指针，不是能力。compact 已改走 opus 以换压缩质量，正因如此「不续指针」必须由枪型（kind）判定——一旦改由模型档（route）代推，旁路摘要会立刻开始污染主会话。反向同理：route 须由 kind 单向推出，两者各判一次优先级会产出 `kind=title` + `route=opus`（按 title 折叠，却按 opus 计价）。
5. **inputs 空串是覆盖，不是缺失。** 主枪全键物化，空串用来盖掉上轮残留的会话变量；见空勿「修」。
6. **计费只看是否进入上游。** Dify 接受流时 LLM 节点已运行，与 HTTP 成败、正文有无分开；只有本地短路不计。账本照实际上游调用计数，勿设免费档。
7. **文档恒走 Read，files 仅图。** 文档进 files 与 CC 的 Read 语义冲突；空回复先查岚的 image 开关与 `workflow_error`，再疑代理。
8. **`@路径` ≠ 全文进岚。** 是否单枪答完取决于 CC 是否在 POST 前把文件预读成 system 轨迹；代理不代读盘（路径穿越、双读、权限前移，已否决）。
9. **附图失败不装看见。** fail-open 继续对话，但必须注 `[[cc_images:failed]]` 且 notes 非空——模型凭上下文猜图是实际发生过的事故。
10. **旁路靠文案指纹识别。** title/recap/compact 的判定依赖 CC 提示词原文；CC 改版可能漏判（旁路误走 opus、误续主会话），排障时先对指纹。user 侧只扫描 string / text 控制文案，不能摊平 `tool_result`——工具正文曾因恰含 title / compact 字样，把普通子代理续写误判成旁路枪。
11. **thinking 签名不可伪造。** Dify 侧无 Anthropic 可校验签名，`signature` 恒空；官方级多轮思考 UI 不可完整模拟，勿当代理丢包去修。
12. **压缩对话区不杀 7272 进程**；改 `.py` 后必须重启 `lan`，否则验的是旧进程。
13. **工具执行前不展示结论。** 文本协议若在 `tool_use` 前先说“已写入”，CC 会先展示该句，随后仍须回传 `tool_result`；结果枪再确认一次，就像回答了两遍。工具枪的可见正文只说明下一步，结论在结果返回后显示一次。
14. **工具正文只归一个逻辑载体。** 当前结果在 query，本轮 `@` 预载在 `Current_Context`，既往结果在 `Tool_invocation`，`History` 只留引用；recap/compact 另用完整历史。容量分片只能进入同名编号槽并保持唯一副本，不能借另一个语义字段作溢出桶；把同一正文同时塞进多处既浪费上下文，也会放大旧状态的权重。
15. **文件上下文是状态，不是档案。** 新的完整 Read/成功 Write，以及当前预载中的完整 Read，取代同路径旧快照；同一路径与同一 offset/limit 的局部 Read 只保留最新成功视图，不同范围互不覆盖，失败结果不取代成功正文。Edit 暂存为“完整基线 + 增量”，直至下次完整状态。缓存必须同步应用成功变更，无法精确应用就失效，宁可重读也不回放旧文件。超过单条上限的正文一律不入缓存——重放不带残缺标记，砍尾比不缓存更坏。路径身份只有一个定义（含 UNC 的前导双斜杠），否则同一文件会在缓存与「最新状态」两套账里各算一个。
16. **并行同层观察，串行一切因果依赖。** 同一阶段路径、参数和必要性均已确定，且没有数据、控制流、共享可变目标或副作用顺序依赖的调用一次列全；同一文件的变更应合并或串行。深度来自阶段间判断，不来自把独立 Read 排成队。
17. **本地确认必须有显式契约。** 只有响应末尾存在一段完整 `after_success`、批次全为目标不冲突的 Write/Edit、session 与全部 tool id 精确匹配且结果命中已知成功形态，才跳过 Dify；任何模糊处都回上游。禁止“看到 Write 就短路”。
18. **超限旧会话不能靠新 inputs 自愈。** Dify 会先恢复持久 conversation variables，再应用本枪输入；旧变量在恢复阶段已超限时，工作流根本不会启动。完成变量分栏后须将受影响的 CC session 与旧 cid 解绑一次，由完整请求在新 conversation 上续接。
19. **上游成功不等于答复送达。** Dify 成功只证明模型工作流完成；须分别核对 Dify 终端事件、代理生成的 `message_stop` 与 ASGI 实际发送结果。总 API timeout 不能替代 idle timeout，但任何固定时长都不能脱离当前配置和传输轨迹被直接认作根因。
20. **自动重试必须合并，不能复刻。** 同一 Anthropic body 的并发重试接驳唯一在途任务，成功后短期回放；断开一个等待者不取消上游。否则一次 6 分钟任务会在第 5 分钟被误判后成倍调用、成倍计费，并让交错的 502 与成功日志无法归属。
21. **`413` 只表示整包实体过大，不代指 Dify 字段边界。** Start 表单的 `/parameters.max_length` 按字符数计；conversation variable 下一轮恢复另受实测 `sys.getsizeof <= 204800` 约束，含一个非 BMP 字符就可能提升整串的内部宽度。后者不是误判，误判在于曾把它包装成 `413`，让 Claude Code 以为数百 KiB 的请求超过 32MB 并启动自动压缩。非 BMP 先做有收益才启用的可逆线缆；任一道已知边界仍越界，一律回 `400 invalid_request_error`，不裁剪、不发送。
22. **保护性机制的删除须先证明其所防故障已经消失。** 编码、兼容、边界、重试、缓存与恢复逻辑即使看来迂回，也先按“触发条件 → 所防故障 → 外部行为 → 测试锚点”建立机制图；注释缺失时应补调查，不能把读不懂当成无作用。涉及外部系统的判断须做包含失败路径的真实 A/B，且多轮故障不能用单轮验证。证据不足时只登记删除候选，不改实现；证据成立后先写能复现旧故障的测试，再删除或替换。
23. **子代理身份必须来自 harness，不能从 prompt 猜。** `agent_id` 由 `SubagentStart` hook 登记并以验签 marker 传输；prompt 哈希、请求序号、工具正文都不是身份。没有可信 marker 时宁可不绑定子代理 CID，也不把一枪误接主会话或另一代理。
24. **完成报告先信消息链，档案只作 fork 兜底。** 可信 `<task-notification><result>` 是主载体；`SubagentStop.last_assistant_message` 只在消息链缺失且能以原 Agent tool-use-id/agent id 关联时恢复一次。档案有界、fail-open，不改写 Claude Code JSONL，也不把“当前 session 无 transcript”扩大为“报告已丢失”。

## Backlog

- **haiku 首读**（省 opus 次数）：`@大文件` 首枪用 haiku 只开 Read、第二枪 opus 分析。
  拦路虎：haiku 不续主会话导致指针分裂；模型未必只 Read。重开前先设计会话策略。
- **结构化工具出口重开**（`TOOL_STRUCTURED=1`）：默认关的因由是插件 prefill 与上游模型
  不兼容；待插件改用 tool 模式后重验。注意出站注入受 `enable_tools` 与 `route==opus`
  双重限制，而入站解析恒开——模型自发吐出 `{reply, tool_calls}` 也会被接住。
- **无 session id 的非流重放边界**：当前 single-flight 指纹含原始请求与上游、用户、
  attachment scope、父 session / agent 命名空间，却不含 sticky 解析后的 CID 或 epoch。
  若要改动，先做“current 切换与同体重试并发”的真实实验，再决定把解析后绑定纳入指纹，
  或明确禁止这类请求跨 current 回放。
- **原生 Messages 路由仅备案，不执行**：设计边界与候选形态见 `架构.md`「远期备案」。
