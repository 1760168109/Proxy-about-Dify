# 架构

> 机制一览。信源优先级：代码 ＞ 本文（冲突时改文档）。
> 安装运行见 `README.md`；教训与守则见 `经验.md`。

---

## 链路

```
Claude Code ── POST /v1/messages（Anthropic Messages）
      │
   代理 lan    判枪 plan → 折叠 parse → 装配 outbound（缓存重放/标记/工具注入/附图）
      │        会话附着 sessions → 计费 meter（上游接受流即记）→ 落盘 log
      ▼
   Dify 岚     POST /chat-messages（query + inputs + files，恒 streaming）
      │        if-else：[[cc_route:haiku]] → Kimi 快支
      │                 [[cc_struct:on]]   → 阿岚·结构化（structured output）
      │                 否则               → 阿岚（散文）
      ▼
   代理 answer  reasoning → thinking · 结构化/标记 → tool_use · 对 CC 出 SSE 或 JSON
      ▼
Claude Code    本机执行工具 → 下一枪带 tool_result 续写
```

tool_result 到达 lan 后先查 `terminal_pending`：若与本 CC session 的显式 Write/Edit
终结批次完全匹配且全部明确成功，lan 本地返回预存答复；其余结果才进入上图的 Dify 链。

边界：代理 = 模型门面（折叠/路由/工具通道/图/账本）；CC = agent harness（执行工具、
权限、UI、读盘）；一次用户意图 = 多枪 HTTP 是 CC 工具环的常态。

## 枪型

| 枪型 | route | tools | 续主会话 | inputs 物化 | 计费 |
|:---|:---|:---|:---|:---|:---|
| 主对话 | opus | 可 | **是** | 全键（空串覆盖旧值） | 计 |
| title / recap / compact | haiku | 关 | 否 | empty（全清） | 计 |
| 子代理·机械 | haiku | 可 | 否 | strip（丢解析键） | 计 |
| 子代理·分析 | opus | 可 | 否 | 全键 | 计 |
| 占位模板（sys/hist/msg） | local | — | — | — | **不计** |
| 工具续写·普通/失败 | 同主档 | 可 | 同主档 | 同主档 | 再计一枪 |
| 工具续写·terminal 成功 | local | 关 | 不访问 Dify | — | **不计** |

判定优先级：旁路指纹 → 子代理（显式 `[[cc_route:]]` / 机械 / 默认 opus）→
`testandlife` → model 名 → 默认 opus。会话附着仅「opus 且非子代理」。

流式裁决：body.stream 优先；省略时看 `Accept: text/event-stream`，否则 JSON
（Anthropic 约定，勿默认 SSE）。上游 Dify 恒 streaming，与对 CC 的形态无关。

## inputs 与 query

瘦 query + 丰 inputs：本轮话进 `sys.query`（首行恒 `[[cc_route:…]]`，其余标记居次），
大文本进会话变量。`INPUT_KEYS` 12 键：claudeMd · Memory · Environment · Language ·
Output_Style · Context_management · CLAUDE · MEMORY · currentDate · Tool_invocation ·
System_Description · History。主对话全键物化——空串是**覆盖**上轮会话变量，不是缺失。

正文来源：`@路径` ≠ 全文进岚。文件正文只经 CC 预读注入（system 轨迹）或工具环
tool_result；代理不代读盘。Wasted call 由 read_cache 重放；仅路径无正文时注
`[[cc_need_read]]` 强制先 Read。

### 上下文纯化

主枪接收的是**当前工作状态**，不是工具时间线的多份复印：

| 材料 | 主枪载体 |
|:---|:---|
| 当前 user 回传的 tool_result 正文 | `sys.query` |
| 既往仍有效的工具正文 | `Tool_invocation` |
| `History` 中的既往 tool_result | 仅保留 id / 工具名 / 路径引用 |
| recap / compact 所需原始时间线 | 内部 `history_full`，只在旁路 query 使用，不与主枪并送 |

岚的三个 LLM 节点均为 `context.enabled: false`，不会从节点会话窗口暗中补回旧正文；
模型所见只由本枪显式写入的 conversation variables 与 `sys.query` 构成。

同一路径以最新完整状态为准：新的完整 Read 或成功 Write 会取代旧 Read 快照及更早的
Write/Edit 大字段。Edit 只是一段增量，故在下一次完整 Read/Write 前保留“最近完整基线 +
Edit old/new”；否则会连未编辑区域一起丢失。read_cache 按消息时间重放成功的 Write/Edit；
若 Edit 无法在缓存快照上精确应用，立即使旧缓存失效，绝不回灌编辑前正文。

## 工具通道（三层，逐级回落）

Dify 不透传 Anthropic `tools[]`。入站把 tools 压成目录 + 协议注入
System_Description（约束居近再加 query 尾注）；出站按优先级解析：

| 通道 | 载体 | 说明 |
|:---|:---|:---|
| **结构化出口** | `node_finished.structured_output` 或整包 `{reply, tool_calls[]}` / `[[cc_tools_json]]` 围栏 | `TOOL_STRUCTURED=1` 且岚有 `[[cc_struct:on]]` 分支；有有效 calls 时解析端强制丢弃 reply，工具关的枪只取 reply。**当前默认关**（插件 prefill 与 opus-4-6 不兼容） |
| **原文块** | `[[tool_use]]` JSON 头 + `[[raw:键]]…[[/raw:键]]` | 长文本零转义（Write.content / Edit old·new / Bash 脚本）——大文件的根治通道；开而未闭整块弃置为可见正文 |
| **JSON 抢救** | 内联 JSON | 严格解析失败后一趟修复：裸换行、CRLF、未转义引号、非法转义（裸 `C:\U…`） |

解析出的调用经参数归一（AskUserQuestion 形状钉死、file_path/command 等别名回填）
→ Anthropic `tool_use` 块 → CC 本机执行。解析失败的块保留为正文（可见失败），
绝不产半调用。

opus 与 haiku/Kimi 节点都必须绑定 `System_Description`，否则快速档看不到工具协议；替换
岚侧节点时须保留该变量。

### 依赖层与批量

工具枪数由**依赖层数**决定，不由工具数量决定。同一认知阶段中，路径、参数、必要性均已
确定，且不存在数据、控制流、共享可变目标或副作用顺序依赖的调用必须一次列全；后项依赖
前项结果或副作用时才进入下一枪：

```text
Read A + Read B + Glob C          → 同层，一枪发出
Glob → 据目录选择 Read → 据正文 Write → 三层，逐枪推进
Write 配置 → 运行依赖该配置的测试   → 有副作用顺序，分两枪
Edit A + Edit A                   → 共享同一文件，合并或串行
```

这是取材动作的并行，不跨越“读材料 → 形成判断 → 再定下一批材料”的认识门槛。代理只解析
模型实际给出的调用，不猜测或补造额外工具；批量宽度可由请求日志的 `tool_count/tool_names`
观察。

### Terminal-tool

文本协议允许目标互不冲突的纯 Write/Edit 终结批次在响应末尾附加一段完整隐藏答复：

```text
[[tool_use]] … Write/Edit … [[/tool_use]]
[[after_success]]全部成功后给用户的简短答复[[/after_success]]
```

`answer` 负责严格解析并剥离该段，`main` 再以实际 tool id 登记到
`data/terminal_pending.json`；初次工具枪不展示它。未闭合、多段、非末尾、结构化 reply
偷渡、同路径重复变更或任何残留协议标记都会在登记前失败关闭。
下一次请求只在以下条件全部满足时由 lan 本地释放：主窗口 · 有 CC session · 当前 user 仅含
精确同一批 tool_result · 全部工具均为 Write/Edit · 每项结果都命中 Claude Code 已知成功
形态。待决记录固定 24 小时过期、最多 64 项；工具相关内容只保存 id / 工具名 / 答复，
不保存参数或文件正文。

登记成功后，任一结果报错、拒绝、未知、缺失、多出结果或混入用户新话，都会消费待决记录
并按普通工具续写交回 Dify。terminal 状态读写异常同样 fail-open，不得截断正常工具链。
成功短路仍按消息时间更新 read_cache，日志
记 `gun_kind=terminal_local` / `skipped_dify=true`，meter 不增。岚节点 context 关闭且下次
CC 请求自带完整消息链，因此跳过确认枪不会造成模型状态断层。

当前 terminal 契约只落在默认文本协议；结构化出口即使意外输出该标记也不会登记。
未来若要支持，须先给 schema 增加独立的 `after_success` 字段，不得复用 `reply`。

工具枪的正文只可说明下一步，不得在工具执行前声称完成或交付最终结论；CC 回传
`tool_result` 后，模型再依据真实结果给一次结论。否则前一枪的提前结论会先被 UI 展示，
工具续写枪又必须确认结果，形成体感上的重复回答。

结构化出口的岚侧 schema：`{reply: string, tool_calls: [{name, input}]}`（两键必填）；
`tool_calls` 非空时 `reply` 必须为空，解析端亦强制执行。分支条件
`sys.query contains [[cc_struct:on]]`，排在 haiku 条件之后。

## 图

仅 image 走 Dify `files[]`（末条 user 顶层 + tool_result 内嵌）；文档恒走 Read。
上传带重试与去重，fail-open：失败不断流，query 注 `[[cc_images:failed]]`，
notes 必非空。岚侧须允许 image 上传，否则常见 File validation → 空回复。

兼容注：旧版 Dify 导出的 `features.file_upload.image.enabled: false` 是历史配置字段，
不能据此单独判定图像通道关闭；以已发布应用的实际上传结果、LLM 节点 vision / `sys.files`
绑定及工作流运行记录为准。

## 会话绑定

`data/sessions.json`：`by_cc[CC session_id] → Dify conversation_id` 是主枪附着的唯一
权威；`current` 只作展示与「请求无 S」时的 sticky（幽灵 current 丢弃）。

| session_bind | 条件 | 出站 |
|:---|:---|:---|
| hit | 见过该 S | 续绑定的 cid |
| miss | 新 S（含 CC 新开对话） | 不传 cid → Dify 新建，成功后 remember |
| missing | 请求无 S | sticky 沿用 current |

resolve 只读；写盘仅 remember / `POST /sessions/new`（解绑）/ `POST /sessions/switch`。

## 计费

按次：chat-messages 被上游接受（HTTP <400 开流）即记一枪——上游 LLM 已运行，与
正文有无分开。本地短路（占位枪、terminal 成功）不计。opus/haiku 单价见 `.env`；这是本地账本，
不是官方账单（token 估算仅供上下文占用感）。

## 标记辞典

| 标记 | 谁写 | 作用 |
|:---|:---|:---|
| `[[cc_route:haiku\|opus]]` | 代理 · query 首行 | 岚 if-else 分档 |
| `[[cc_struct:on]]` | 代理 | 路由到结构化出口分支 |
| `[[cc_tool_continue]]` | 代理 | 工具续写壳 |
| `[[cc_tools:on]]` | 代理 · query 尾 | 工具提醒（约束居近） |
| `[[cc_need_read]]` | 代理 | 引用路径但无正文 → 须先 Read |
| `[[cc_images:N\|failed]]` | 代理 | 附图成功 / 失败（勿装看见） |
| `[[cc_read_cache:miss]]` | 代理 | Wasted call 无缓存可重放 |
| `[[tool_use]]` / `<tool_use>` / `<tool_call>` | 模型 | 文本协议工具块 |
| `[[raw:键]]…[[/raw:键]]` | 模型 | 零转义原文块（以块为准覆盖同名键） |
| `[[after_success]]…[[/after_success]]` | 模型 → 代理暂存 | 仅 Write/Edit 全成功后本地释放的终结答复 |
| `[[cc_tools_json]]…` | 模型 | 结构化外壳围栏形态 |
| `testandlife` | 用户 | 强制 haiku 冒烟 |

## 已知不可为

- thinking `signature` 恒空：Dify 无 Anthropic 可校验签名，官方级多轮思考 UI 不可完整模拟。
- `max_tokens` 等入站采样参数被忽略（上游节点自配）。
- 官方订阅 `/usage` 美元与本地按次账本是两条线，勿互相换算。

## 远期备案：原生 Messages（未执行）

本代理成立的主要前提是 Dify 专业计划提供只能经 Dify 使用的模型额度；故现阶段不增加
外部 Messages 上游、密钥或路由。若未来因个性化设计另有真 Anthropic Messages 上游，
再在两种形态中择一：① CC Switch 新建独立 Native 配置直接切换；② lan 以显式 model
alias 分流，工具密集枪走 native、纯聊留岚。不得仅因请求含 `tools[]` 自动漂移上游。

原生通道的价值是 tools 结构原样透传并省去文本协议；费用按该上游规则另计。它本身不会消除
Read→tool_result→Write 的模型依赖轮次。此项当前只有设计备案，无代码、配置或 Dify
工作流改动。

## 北极星

在 Dify 额度约束内，工具执行留在 CC，lan 负责忠实转译、同层批量、上下文纯化，并以
显式 terminal 契约消除可证明多余的成功确认枪；不以猜测短路换取表面上的少一轮。
