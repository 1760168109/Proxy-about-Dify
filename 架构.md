# 架构

> 机制一览。信源优先级：代码 ＞ 本文（冲突时改文档）。
> 安装运行见 `README.md`；教训与守则见 `经验.md`。

---

## 链路

```
Claude Code ── SubagentStart/Stop hook + POST /v1/messages（Anthropic Messages）
      │
   代理 lan    判枪 plan → 折叠 parse → 装配 outbound → 调 Dify
      │        沿途：sessions 会话附着 · meter 计费 · log 落盘（时点见下节）
      ▼
   Dify 岚     POST /chat-messages（query + inputs + files，恒 streaming）
      │        if-else：[[cc_route:haiku]] → Kimi 快支
      │                 [[cc_struct:on]]   → 阿岚·结构化（structured output）
      │                 否则               → 阿岚（散文）
      ▼
   代理 answer  reasoning → thinking · 结构化/标记 → tool_use · 对 CC 出 SSE 或 JSON
      ▼
Claude Code    本机执行工具 → 下一枪带 tool_result 续写
```

tool_result 到达 lan 后，**判枪之后、折叠装配之前**查 `terminal_pending`（须先判枪才知道
这是不是主窗口枪）：若与本 CC session 的显式 Write/Edit 终结批次完全匹配且全部明确成功，
lan 本地返回预存答复；其余结果才进入上图的 Dify 链。

### 一枪之内的顺序

hook marker 验签/剥除 → 判枪（`build_plan`，一次扫 body）→ terminal 待决查询（仅主窗口）→ 占位模板/状态枪本地短路 →
折叠 `parse` → 装配 `outbound`（缓存摄取与重放 → inputs 物化 → 工具注入 → query 组装
与标记 → Agent 生命周期块 → Unicode 线缆 → 已发布编号槽分片 → Dify 两道输入边界预检）→ 请求日志落盘 → 附图 → 会话锁内取
conversation_id → 调 Dify（HTTP <400 即计费，早于读流）→ 译流 → 收尾写回绑定与日志。
非流请求在调 Dify 前多一层 single-flight 指纹查询，流式请求没有这一层。

边界：代理 = 模型门面（折叠/路由/工具通道/图/账本）；CC = agent harness（执行工具、
权限、UI、读盘）；一次用户意图 = 多枪 HTTP 是 CC 工具环的常态。

## 枪型

| 枪型 | route | tools | attachment scope | inputs 物化 | 计费 |
|:---|:---|:---|:---|:---|:---|
| 主对话 | opus | 可 | `main` | 全键（空串覆盖旧值） | 计 |
| title / recap | haiku | 关 | `none` | empty（全清） | 计 |
| compact | **opus** | 关 | `none` | empty（全清） | 计 |
| 子代理·机械 | haiku | 可 | `subagent`（有身份时） | strip（丢全部 13 键） | 计 |
| 子代理·分析 | opus | 可 | `subagent`（有身份时） | 全键 | 计 |
| 子代理动作状态 | local | 关 | `none` | — | **不计** |
| 占位模板（sys/hist/msg） | local | — | — | — | **不计** |
| 工具续写·普通/失败 | 同主档 | 可 | 同枪 scope | 同主档 | 再计一枪 |
| 工具续写·terminal 成功 | 同主档 | 关 | 不访问 Dify | — | **不计** |

判定优先级：占位模板 → 已确认子代理动作状态指纹 → 旁路指纹 → 子代理（显式 `[[cc_route:]]` / 机械 / 默认 opus）→
`testandlife` → model 名 → 默认 opus。这串里**只有占位模板是本地短路**；「旁路」指不占
主会话窗口的摘要枪（title / recap / compact），它们照样打上游、照样计费，只是不续会话
指针——只有 `attachment_scope=main` 的主窗口枪续主 CID；旁路摘要即使走 opus 也不续。

**模型档（route）与枪型（kind）是两件事。** compact 独走 opus 是为压缩质量买单（压坏则
后续全程受影响），title / recap 只需短产出，留在快档省额度；但旁路枪的 inputs 物化档、
query 载体、不续主会话指针一律由 kind 定，与走哪个档无关。旁路 ⟹ haiku 这个恒等式自
compact 改档起不再成立，故任何策略都不得再由 route 代推。

判枪时 **kind 先定，旁路枪的 route 由 kind 单向推出**：title 指纹与 compact 兜底指纹
交叉命中时 kind 以 title 为准、route 随之为 haiku，`route_reasons` 追加
`compact_marker_overridden` 留证。若改回两者各判一次优先级，同一请求会得到 `kind=title`
+ `route=opus`——按 title 折叠却按 opus 计价。

压缩类的兜底指纹（`compact the conversation` / `Summarize this conversation`，双 ESC
触发）产出与真 compact 完全一致的枪型，不是只改 route 的第四类。

三个易混布尔的构成各不相同，勿互推：

| 布尔 | 构成 | 管什么 |
|:---|:---|:---|
| `enable_tools` | 请求带非空 `tools[]` 且非旁路 | 给不给工具目录与协议注入 |
| `attachment_scope` | `main` / `subagent` / `none` | 选择主 CID、子代理 CID 或不附着 |
| `attach_main` | `attachment_scope == "main"`（兼容派生字段） | 兼容旧日志/调用方，不是真源 |
| `is_main_window` | 非子代理且非旁路（**不看 route**） | 算不算主窗口：查 terminal 待决、携带 Agent 生命周期 |

故 `testandlife` 冒烟枪、model 名含 haiku 的主对话枪都是主窗口，一样会查 terminal 待决；
动作状态枪是 `none`，不查子代理锁也不进入 Dify。

### 子代理身份、CID 与报告

Claude Code 的 `SubagentStart` hook 收到 `session_id`、`agent_id`、`agent_type` 后，代理在
`data/agents.json` 登记一个有界身份记录，并返回带 HMAC 的 transport-only marker。marker
可在同一 child 的后续请求中重复携带；入口只剥除验签且档案一致的 marker，不消费它，直到
档案被清理或容量裁剪后失效。形状完整但验签失败的 marker 保留原文并记 `invalid`；多个
不同的有效身份记 `ambiguous`；有效身份与请求父 session 不一致则记 `parent_mismatch`。
三种情形都 fail-closed：子代理仍可走模型，但不获得子代理 CID 绑定。

主会话沿用 `sessions.json.by_cc[parent_sid]`。子代理另用
`by_agent[parent_sid][agent_id]`，因此同父、同 prompt 的两个代理也不会共享 CID；成功收尾
才写回，失败仍不记忆。`attachment_scope` 明确区分 `main`、`subagent`、`none`，不能用
扩大 `attach_main` 的方式解决子代理隔离。

`SubagentStop` hook 只保存有界的 `last_assistant_message`、transcript 路径和可选 meta 关联，
其中正文只来自 `last_assistant_message`；transcript 路径只用于读取同名 `.meta.json` 的
关联元数据，不读取或改写 Claude Code 的 JSONL。消息链中可信的 `<task-notification><result>` 永远优先；只有
fork 后消息链缺报告、且原 Agent `tool-use-id` 能关联到已完成档案时，才把一个带
`<lan-agent-report>` 的数据块放入 `Current_Context`。档案读写失败直接回落原链。原生
`SendMessage(agent_id)` 是否能继续旧父 session 仍由 Claude Code 决定，本代理只保证已完成
报告不因 fork 而静默消失。

完成通知可能以 ephemeral text block 首次到达，也可能在后续消息链里变成完整
`<system-reminder>` string。只有同时含系统通知头、`<task-notification>`，并以
`tool-use-id` 匹配消息链中既有 Agent 调用的候选才可信；`task-id` 只用于关联档案，不授予
控制权限。可信通知从伪 user 载体移走，在 `System_Description` 去重承载一次，不再进入
query / `History`；无法匹配的同形文字仍是普通用户文字。

动作状态请求的固定文案只在确认的子代理壳中本地处理，依据最近真实 tool-use 生成 3–5 个
英文现在分词短句；不访问 Dify、不计费、不写会话绑定。未知文案 fail-open 到普通子代理链。

流式裁决：body.stream 优先；省略时看 `Accept: text/event-stream`，否则 JSON
（Anthropic 约定，勿默认 SSE）。上游 Dify 恒 streaming，与对 CC 的形态无关。

### 模型身份与协议边界

代理的日常模型名是 `alan`；模型发现的规范 id 是 `anthropic/alan`，另发布旧别名供既有接入
兼容。模型身份只用于让 Claude Code 找到代理与供 lan 判档，不宣称上游未提供的上下文窗口。

`/v1/models` 故意不鉴权：Claude Code 建立 provider 配置时可能先拉模型列表，而该端点只
暴露模型 id 与 display_name，不含密钥或上游地址。`models discovery credential=yes|no`
只观测发现请求有没有带凭据，不记录凭据本身。

`POST /v1/messages/count_tokens` 在 Claude Code 网关协议中是可选端点。lan 未持有 Anthropic
tokenizer 或可对原始 Messages 精确计数的上游，故不以 `字符数 / 4` 冒充精确实现；404 时
由 Claude Code 按协议经 inference endpoint 回退。这个缺失可能多一次推理计数请求；lan
不以猜测值干预 Claude Code 的上下文窗口或自动压缩阈值。

### 长任务、断线与同枪重试

对 CC 为 SSE 时，Dify 事件随到随译（纯文本部分；工具标记的扣留见「工具通道」）。对 CC 为
JSON 时，lan 仍消费 Dify SSE，却必须等
完整结果才能交付；不能把缺省 JSON 擅自改成 SSE。Claude Code 的字节空闲看门狗独立于
总 API timeout：前者覆盖最长无事件空窗，后者覆盖任务总时长（键名与配置位置见
`README.md`）。

模块边界也以 EOF 为准：`dify.py` 只从单行 `data:` 中拆出 JSON 并逐事件原样交给下游，
空 data、`[DONE]` 与非 JSON 行忽略；`message_end` / `workflow_finished` 只记为终端事件，
不提前截断 HTTP 流。正文、思考、工作流状态、结构化输出与 CID 则由 `answer.py` 累加并在
自然 EOF 后统一 finalize；无错误且已见 CID 时，即使没有 `message_end` 也可能写回绑定，
完整性须回到传输轨迹判断。

#### 传输观测边界

长任务的“成功”分为三个不可互换的事实层：

| 观测层 | 代表事件 | 能证明什么 | 不能证明什么 |
|:---|:---|:---|:---|
| Dify 上游 | `dify_response_headers`、`dify_first_event`、`dify_terminal_event`、`dify_stream_closed` | Dify SSE 是否建立、收到过什么事件、是否正常 EOF，以及解析器看到的最后事件 | Claude Code 是否收到答复 |
| lan 转译器 | `downstream_chunk_generated`、`downstream_stream_generated_complete` | 代理是否生成了 Anthropic SSE，以及是否生成 `message_stop` 或 `error` | socket 写入是否成功 |
| ASGI 下游 | `downstream_*_send_attempt`、`downstream_*_sent`、`downstream_*_send_error`、`downstream_response_finished` | 服务器是否尝试并成功调用 ASGI `send`，以及写入在哪一块失败 | Claude Code 是否解析、显示或持久化该块 |

每枪请求 JSON 旁的同名 `.trace.jsonl` 是追加式轨迹；关键边界事件强制刷盘，普通分片按
样本记录，避免观测本身拖慢长思考。轨迹不保存 SSE `data` 正文或鉴权头；
`dify_event_yielded` 的“yielded”只指交给转译器，不能解释为“已送达”。
`GET /debug/last-request` 返回最近 40 条轨迹，完整文件名在 `summary.transport_trace_file`。
若主 JSON 缺少最终 `response`/`delivery_status`，仍应先查轨迹尾部：进程可能在请求收尾前被
取消或终止。

取证顺序固定为：先看 Dify 是否有终端事件，再看转译器是否生成 `message_stop`，最后看
该块的 ASGI `send` 是否成功。只有三层都完成，才可称为“代理已完成交付”；仍不能据此
断言 Claude Code 已消费，后者须结合客户端调试日志或下一枪的行为判断。

流式请求的摘要状态只概括转译器收尾，不替代完整轨迹：

| `delivery_status` | 含义 |
|:---|:---|
| `stream_complete` | 正常生成 `message_stop`，且其 ASGI `send` 已返回；仍须看 `downstream_response_finished` 判断最终空 body 是否收口 |
| `stream_error_delivered` | 上游/协议错误已被转换为 Anthropic `error` SSE 并写出 |
| `stream_complete_without_message_stop` | 生成器自然结束但未生成 `message_stop`，属于协议异常信号 |
| `client_disconnected` | 生成器未完成，且断连探针确认客户端已离开 |
| `stream_incomplete` | 生成器未完成，但断连探针未能确认客户端离开；结合异常与 ASGI 轨迹继续定位 |

这里的几个“完成”词不在同一层：`dify_stream_closed.completed` 只表示上游读到 EOF，
`downstream_stream_generated_complete` 表示转译器生成完分片，`downstream_response_finished.completed`
表示 ASGI 响应正常收口；它们都不能单独证明 Claude Code 已解析或显示。`on_accepted`（日志
中的上游接受点）只表示 Dify 已接受 HTTP 流，可能已经计费，也不表示正文或下游交付。

非流请求以完整 Anthropic body 加本上游命名空间生成 SHA-256 指纹，日志只写前缀。进程内
single-flight 状态如下：

| 状态 | 含义 | Dify 调用 / 计费 |
|:---|:---|:---|
| `start` | 本指纹没有在途或可回放结果，创建唯一上游任务 | 1 次 |
| `join` | 同体重试到达时唯一任务仍在运行，接驳等待 | 0 次新增 |
| `replay` | 唯一任务已成功，180 秒保留期内回放结果 | 0 次新增 |

等待者以 shield 接驳：某个 HTTP 客户端超时、撤回或断线，只改变该等待者的
`delivery_status`，不会取消共享 Dify 任务。失败不缓存，下一次可重试；成功只保留 180 秒
且只留最近 32 个指纹（若出现更多不同的非流成功，最旧者提前逐出），以免把稍后有意重复的
用户消息永久判成重试。`upstream done` 只证明
Dify → lan 收集完成；`delivery_status=ready` 才表示返回时客户端仍连接，二者不可合并判断。

## inputs 与 query

瘦 query + 丰 inputs：本轮话进 `sys.query`（首行恒 `[[cc_route:…]]`，其余标记居次），
大文本进会话变量。`INPUT_KEYS` 13 键：claudeMd · Memory · Environment · Language ·
Output_Style · Context_management · CLAUDE · MEMORY · currentDate · Tool_invocation ·
Current_Context · System_Description · History。全键物化时，空串是**覆盖**上轮会话
变量，不是缺失。

上述 13 键是语义基键。Dify 若发布 `Tool_invocation_1…N`、`Current_Context_1…N` 或
`History_1…N`，它们只作为同名基键的容量分片，不形成新的材料类别；代理从 `/parameters`
动态发现，并把所有未用槽也物化为空串。当前本机部署基线是私有文件
`C:\Users\杨\Desktop\Key chain\AI 装置\本地代理\岚.yml`（已由 `.gitignore` 排除，
不进入公开仓库），固定发布
`Tool_invocation` 十一槽，`History` 一槽，`Current_Context` 两槽；分片时基字段本身是第 0 片。

正文来源：`@路径` ≠ 全文进岚。文件正文只经两条路到达，代理不代读盘：

- **CC 在 POST 前预读注入**：线缆上是 `messages` 里 `role=="system"` 的消息，正文裹在
  `Called the Read tool with the following input: …` 这段英文叙事内；落 `Current_Context`。
- **工具环 tool_result**：标准 tool_result 块；当前的落 `sys.query`，既往的落 `Tool_invocation`。

两条路的 wasted call 处置**不同**。所谓 wasted call，是 CC 判定该文件自上次 Read 未变时
不重传正文、只回一句 `Wasted call: file unchanged since your last Read.`——read_cache 存在
的首要理由就是接住它。tool_result 形态由 read_cache 重放（无快照可放才注
`[[cc_read_cache:miss]]`）；预读注入形态**不参与重放**——`Current_Context` 不在重放的三个
参数内，只被当作 read evidence 用于抑制标记，故改注 `[[cc_need_read]]` 逼一次真 Read。
仅路径无正文时同样注 `[[cc_need_read]]`。

### 上下文纯化

主枪接收的是**当前工作状态**，不是工具时间线的多份复印：

| 材料 | 主枪载体 |
|:---|:---|
| 当前 user 回传的 tool_result 正文 | `sys.query` |
| 当前 user 之后由 CC 注入的 `@` 预载 system 轨迹 | `Current_Context` |
| 既往仍有效的工具正文 | `Tool_invocation` |
| `History` 中的既往 tool_result | 仅保留 id / 工具名 / 路径引用 |
| recap / compact 所需原始时间线 | 无——parse 只交出消息原料，由旁路装配时按需折叠 |

岚的三个 LLM 节点均为 `context.enabled: false`，不会从节点会话窗口暗中补回旧正文；
模型所见只由本枪显式写入的 conversation variables 与 `sys.query` 构成。

上表只适用于 opus 主枪与 opus 子代理枪，且 `Current_Context` 与 `Tool_invocation` 的分流
**只看下标**——同一段 `role=="system"` 工具轨迹，位于收尾 user 之后进前者、之前进后者，
不按材料语义区分。

其余枪型的 `@` 预载去向不同：`trim_mode="strip"` 的 haiku 枪丢弃全部 INPUT_KEYS（含
`Current_Context`）；recap / compact 同样丢弃（`build_history_and_current` 跳过
`role=="system"`）；title 枪反过来把预载整段折进 query。

正文被丢是 `strip` 的设计，但那一枪**必须知道正文可以要**，否则既没有材料也不知道去取。
故 `[[cc_need_read]]` 的判据取**物化后**的 inputs（真正发出去的那份），非旁路枪一律参与
判定——取物化前的稀疏值会认定「已有正文」而咽下提示。旁路枪例外：它恒 13 键空串，本就
不该收 Read 指令。

同一路径以最新完整状态为准：新的完整 Read、成功 Write，以及本轮 `Current_Context` 中无
offset/limit 的完整 Read，会取代旧 Read 快照及更早的 Write/Edit 大字段。Edit 只是一段
增量，故在下一次完整 Read/Write 前保留“最近完整基线 + Edit old/new”；否则会连未编辑
区域一起丢失。read_cache 按消息时间重放成功的 Write/Edit；若 Edit 无法在缓存快照上精确
应用，立即使旧缓存失效，绝不回灌编辑前正文。超过单条上限的正文不入缓存——砍尾重放
不带残缺标记，比不缓存更坏。

局部 Read 另按“规范化路径 + offset + limit”形成读取视图。同一视图重复读取时，只保留
最新成功、非空且非 Wasted-call 的结果；旧结果仍保留 tool id 与范围引用。不同范围互不
覆盖，后一次失败也不能淘汰前一次成功正文。这样保留状态变化的证据，而不让近似相同的
大段正文随每次核查重复累积。

缓存摄取（`ingest`）与重放同受一个闸：旁路与占位枪都不跑，其余枪在装配入口先摄取后重放
——扫整条 messages 链，成功 Read 入快照、Write 换快照、Edit 能精确应用则应用、否则弃掉
该快照，靠 `mutation_id` 保幂等，同一个 Edit 在后续重扫时不会被重复应用。

路径身份由单一归一函数定义（去引号、剥 `@`、斜杠归一、折叠重复分隔符、casefold，
且**保留 UNC 前导双斜杠**）。缓存键与「最新完整状态」键必须同出此处，否则同一文件会在
两套账里各算一个。

### 旁路枪的 query 折叠

旁路枪不收 inputs（13 键全空串），历史只能进 `sys.query`。但「折叠」在两档里不是同一件事：

| | title（`title_fold`） | recap / compact（`history_current`） |
|:---|:---|:---|
| 函数 | `fold_messages_to_query(body)` | `fold_history_current_to_query(history, query_user)` |
| 顶层 system | **含**，作为 `[system]` 段整块进 query | **不含**——防 CC harness 提示灌进压缩产物 |
| `messages` 里的 `@` 预载（`role=="system"`） | **含**（不按 role 过滤） | **不含**（`build_history_and_current` 跳过 system） |
| `<system-reminder>` | 不剥 | 已剥 |
| 历史里的 tool_result | 完整正文，不带 `tool_use_id` | 完整正文，带 id（旁路不传 `compact_tool_results`，与主枪 History 只留引用相反） |

旁路枪一律跳过：read_cache 摄取与重放 · 附图（即便这一轮贴了图，`image_upload_status=skipped`）·
`[[cc_need_read]]` · Agent 生命周期块（只对主窗口 `kind=chat`）。
inputs 恒为 13 个空串，且旁路枪跳过应用参数读取；无论 query 多大，都不触发 input 字段预检。

### Dify 两道输入边界

Dify 对同一个 input 存在两套不同计量，不能互相代替：

| 阶段 | 边界 | 计量 |
|:---|:---|:---|
| Start 表单接收本枪 inputs | 应用 `GET /parameters` 的 `user_input_form.max_length` | Unicode 字符数 `len` |
| 下一枪恢复 conversation variables | 当前 Dify 恢复路径实测上限 `204800` | CPython 字符串内存 `sys.getsizeof` |

第二道边界经隔离双轮验证：`"x" * 52000 + "💡"` 共 52,001 字符，首轮成功写入；第二轮在
同一 conversation 以空 inputs 续接时返回 `variable size 208064 exceeds limit 204800`。把其中
的非 BMP 字符可逆表示为 `⟦U+01F4A1⟧` 后，占用降为 104,078，两轮均成功。因此内存边界
不是 HTTP 请求体大小，也不是 Start 表单字符上限，却确实存在于会话恢复阶段。

lan 的最终次序是：inputs 完成物化、缓存重放与工具注入后，逐字段判断非 BMP 线缆是否真的
降低 `sys.getsizeof`；任一字段启用时，统一转义字面量 `⟦`，向 query 注入解码约定，并在
Dify 答复完成工具协议解析后还原正文、思考与工具参数。若三个大字段仍超限，则在完整保留
线缆文本的前提下进入已发布的同名编号槽；每片以 `190000` 字节为装配目标、以 `204800`
为最终硬边界。然后先按最终字段的字符数校验表单边界，再校验每个持久 input 的内存占用。
线缆不节省内存时保持原文，不为使用编码而编码。

2026-08-25 事故负载的隔离双轮验证：原 `Tool_invocation` 在线缆后为 119,236 字符、
238,530 字节；临时分作 94,158 与 154,028 字节的两个变量后，首轮运行成功，变量接口读回
一致，同一 conversation 空 inputs 第二轮恢复也成功，测试 conversation 随后删除（204）。
这证明恢复限制逐变量生效，也证明增加表单 `max_length` 不能替代分片。

历史基线（四槽 DSL）发布后的复验已通过：`/parameters` 按当时版本发布 25 个输入；事故原请求经 lan
进入 opus，`Tool_invocation_1/2` 分别为 190,000 与 48,588 字节，工作流成功且答复送达。
当前 DSL 已将物理配额重排为工具调用十一槽、历史一槽、当轮上下文两槽；基字段参与第 0 片，
导入并发布后，旧分组的会话应以新 conversation 重新建立。
隔离 conversation 首轮从变量接口读回的两片与代理装配逐字一致；第二轮显式携带同一 CID、
以当时 DSL 的 25 个空输入续接，恢复成功并把当时的 `Tool_invocation` 基键及四个编号槽全部覆盖为空。
测试 conversation 与本地测试绑定均已清理。以上两句是历史四槽证据，不是当前 11+1+2 分片的验收结果；
当前部署须按上方新槽位重新发布并复验。

表单参数按 `DIFY_BASE_URL + API key` 指纹隔离并缓存五分钟：

- 刷新成功：采用当前发布值；刷新失败但有旧值时短期沿用并记 `limit_src=stale`。
- 刷新失败且没有旧值：仅表单边界 fail-open，由 Dify 自行校验；不能猜一个 `max_length`。
- 持久变量的 `204800` 是当前恢复路径已复现的已知边界，不随参数读取失败而消失。
- 任一道已知边界越界：返回 Anthropic 形状的 `400 invalid_request_error`，不调用 Dify、
  不计费、不裁剪，并记录字段、实际值及对应上限。

输入边界不能返回 `413`。Claude Code 将 `413 Request Entity Too Large` 解释为整份 Anthropic
请求超过其 32MB 限制，并进入附件清理或压缩恢复流程；这里的请求可能仅数百 KiB，错误语义
完全不同。

`sys.query` 既不是 `user_input_form` 字段，也不是 conversation variable，lan 不为它虚构本地
阈值；若 Dify 对 query 另有限制，以实际上游错误为准。read_cache 的
`max_chars_per_entry = 2_000_000` 是本地磁盘快照容量边界，也与上述两者无关。

#### 线缆层变更门槛

线缆层是一个不可只拆一半的跨模块契约：① 按收益选择非 BMP 表示；② 转义字面量 `⟦`；
③ 向模型声明表示法；④ 在正文、思考、工具参数和 SSE 分片中还原；⑤ 对最终 inputs 做
`204800` 持久变量预检。缺少任一项，都可能分别造成次轮恢复失败、字面文本被误解或线缆表示
泄漏给 Claude Code。

删除或替换其中任一职责，必须同时满足：

1. 在当前已发布 Dify 应用上做隔离 A/B；每组均含 fresh conversation 首轮和同 cid 空 inputs
   第二轮，不能以单轮成功代替。
2. 对照至少覆盖“长 BMP 文本 + 一个非 BMP 字符”、字面量 `⟦U+hhhhhh⟧`、SSE 跨分片及
   工具参数；记录字符数、`sys.getsizeof`、两轮 HTTP 与 Dify 错误正文。
3. 先建立能在旧故障上失败的回归测试，再改变实现；替代方案须让同一测试转绿。
4. 若无法取得真实上游证据，只能登记为待核验删除候选，不得据“请求远低于 32MB”或
   “`max_length` 尚有余量”直接删除。

当前自动锚点位于 `tests/test_unicode_wire.py`（表示可逆）、`tests/test_outbound.py`
（分片、清槽与容量不足）及 `tests/test_precepts.py`（119k 事故拒绝与分片端点往返）。

## 工具通道（三层，逐级回落）

Dify 不透传 Anthropic `tools[]`。入站把 tools 压成目录 + 协议注入
System_Description（约束居近再加 query 尾注）；出站按优先级解析：

| 通道 | 载体 | 说明 |
|:---|:---|:---|
| **结构化出口** | `node_finished.structured_output` 或整包 `{reply, tool_calls[]}` / `[[cc_tools_json]]` 围栏 | 出站注入需 `TOOL_STRUCTURED=1` 且本枪 `enable_tools` 且 `route==opus`；工具关的枪只取 reply。**出站默认关**（重开条件见 `经验.md` Backlog），但**入站解析恒开**——模型自发吐出该形态也接得住 |
| **原文块** | `[[tool_use]]` JSON 头 + `[[raw:键]]…[[/raw:键]]` | 长文本零转义（Write.content / Edit old·new / Bash 脚本）——大文件的根治通道；`[[raw:]]` 开而未闭则整块弃置为可见正文，而 `[[/tool_use]]` 缺失是**宽容**的（模型常漏，仍认块） |
| **JSON 抢救** | 内联 JSON | 严格解析失败后一趟修复：裸换行、CRLF、未转义引号、非法转义（裸 `C:\U…`） |

解析出的调用经参数归一（AskUserQuestion 形状钉死、file_path/command 等别名回填）
→ Anthropic `tool_use` 块 → CC 本机执行。解析失败的块保留为正文（可见失败），
绝不产半调用。

结构化出口的岚侧 schema：`{reply: string, tool_calls: [{name, input}]}`（两键必填）；
`tool_calls` 非空时 `reply` 必须为空，解析端亦强制执行。分支条件
`sys.query contains [[cc_struct:on]]`，排在 haiku 条件之后。

opus 与 haiku/Kimi 节点都必须绑定 `System_Description`，否则快速档看不到工具协议；替换
岚侧节点时须保留该变量。

流式下的扣留：SSE 是随到随译，但工具标记不能半块发出。译流时尾部保留 32 字符不发
（`_HOLD`），一旦 `find_stream_cut` 命中 `STREAM_CUT_MARKERS`（含 ``` 代码围栏）即锁定
`tools_locked` 并停止继续吐正文，收齐后统一拆块。

### 依赖层与批量

工具枪数由**依赖层数**决定，不由工具数量决定。同一认知阶段中，路径、参数、必要性均已
确定，且不存在数据、控制流、共享可变目标或副作用顺序依赖的调用必须一次列全；后项依赖
前项结果或副作用时才进入下一枪：

```text
Read A + Read B + Glob C          → 同层，一枪发出
Glob → 据目录选择 Read → 据正文 Write → 三层，逐枪推进
Write 配置 → 运行依赖该配置的测试   → 有副作用顺序，分两枪
Edit A + Edit A                   → 共享同一文件，合并或串行
```

这是取材动作的并行，不跨越“读材料 → 形成判断 → 再定下一批材料”的认识门槛。代理只解析
模型实际给出的调用，不猜测或补造额外工具；批量宽度可由请求日志的 `tool_count/tool_names`
观察。

### Terminal-tool

文本协议允许目标互不冲突的纯 Write/Edit 终结批次在响应末尾附加一段完整隐藏答复：

```text
[[tool_use]] … Write/Edit … [[/tool_use]]
[[after_success]]全部成功后给用户的简短答复[[/after_success]]
```

`answer` 负责严格解析并剥离该段，`main` 再以实际 tool id 登记到
`data/terminal_pending.json`；初次工具枪不展示它。未闭合、多段、非末尾、结构化 reply
偷渡、同路径重复变更或任何残留协议标记都会在登记前失败关闭。失败原因分**三层**各自具名：
解析层判语法、资格层判「这批工具配不配得上短路」，两者都记 `after_success_reason`；存储层
判「这条记录存不存得下」，由 `register` 自己命名并记 `terminal_pending_reason`。三层各自
可辨，「为何回上游」才查得出。资格层的 `misused_without_tools` 另带一个动作：标记误用在
无工具的普通答复上时，把草案**拼回可见正文**，不吞掉那段字。
下一次请求只在以下条件全部满足时由 lan 本地释放：主窗口（**不要求 opus 档**，定义见「枪型」）·
有 CC session · 当前 user 仅含精确同一批 tool_result · 全部工具均为 Write/Edit ·
每项结果都命中 Claude Code 已知成功形态。待决记录固定 24 小时过期、
最多 64 项；工具相关内容只保存 id / 工具名 / 答复，不保存参数或文件正文。

登记成功后，只要有结果报错、拒绝或呈未知形态，或结果集缺失、多出、混入用户新话，都会消费
待决记录并按普通工具续写交回 Dify；但实际结果 id 与登记集合**完全无交集**时（这一枪根本
不是那批工具的回传）记录不消费，继续等它真正的结果枪。成功消费后对同一批 `tool_result` 保留指纹
回放，防止并发重试再次调用 Dify——回放期与待决同为 24 小时，并不更短（容量见「落盘」）；
回放仍不保存参数或正文。terminal 状态读写异常同样 fail-open，
不得截断正常工具链。

读这条链时要把两枪分开：第一枪只送出 `tool_use` 并登记 `after_success`；第二枪带回
同一批 `tool_result`，只有全部是明确成功的 Write/Edit 才释放预存答复。失败、混入新话或
结果集合不匹配都会回到普通 Dify 续写。
成功短路仍按消息时间更新 read_cache，日志
记 `gun_kind=terminal_local` / `skipped_dify=true`，meter 不增。岚节点 context 关闭且下次
CC 请求自带完整消息链，因此跳过确认枪不会造成模型状态断层。

当前 terminal 契约只落在默认文本协议；结构化出口即使意外输出该标记也不会登记。
未来若要支持，须先给 schema 增加独立的 `after_success` 字段，不得复用 `reply`。

工具枪的正文只可说明下一步，不得在工具执行前声称完成或交付最终结论；CC 回传
`tool_result` 后，模型再依据真实结果给一次结论。否则前一枪的提前结论会先被 UI 展示，
工具续写枪又必须确认结果，形成体感上的重复回答。

## 图

仅 image 走 Dify `files[]`（末条 user 顶层 + tool_result 内嵌）；文档恒走 Read。
上传带重试与去重，保留“原图索引 → files 索引”的映射；中间失败或去重不会把后图冒充前图。
fail-open：失败不断流，query 注 `[[cc_images:failed]]`，notes 必非空。岚侧须允许
image 上传，否则常见 File validation → 上游空产出。**「空产出」不等于 CC 看到空白**：正文、
思考、工具三者全空时代理会合成一段中文诊断正文，必带事件计数 `events=N`；上游另报了错则加
`status=` / `error=`，并附一句「若 error 含 File validation：检查岚的文件上传/图片开关
并重新发布」。

兼容注：旧版 Dify 导出的 `features.file_upload.image.enabled: false` 是历史配置字段，
不能据此单独判定图像通道关闭；以已发布应用的实际上传结果、LLM 节点 vision / `sys.files`
绑定及工作流运行记录为准。

## 会话绑定

`data/sessions.json`：`by_cc[CC session_id] → Dify conversation_id` 是主枪附着的唯一
权威；`by_agent[parent session][agent_id] → Dify conversation_id` 是子代理附着的独立命名空间。
`current` 只作展示与「请求无 S」时的 sticky。已有 `by_cc` 时，不在其映射中的 current
视为幽灵并丢弃；`by_cc` 为空时允许 current-only sticky。子代理 CID 不改变主 current。

| session_bind | 条件 | 出站 |
|:---|:---|:---|
| hit | 见过该 S | 续绑定的 cid |
| miss | 新 S（含 CC 新开对话） | 不传 cid → Dify 新建，成功后 remember |
| missing | 请求无 S | sticky 沿用 current |

`session_bind` 只描述会话查找，不描述交付：`hit`/`miss` 读的是主 `by_cc[S]` 或子代理
`by_agent[parent][agent]`，`missing` 是请求没有 S 时尝试 sticky，`skip`（日志中可能出现）
则表示本枪 `attachment_scope=none`，根本没有做会话查找。

并发写回使用三类护栏：`binding_epoch` 是 current 的选择序号，`scope_epochs[session:S]`
是某个 parent session（主映射与其子代理共用）的定向 fence，`binding_reset_epoch` 是
`clear_all` 的总闸。旧调用只传 `binding_epoch` 仍按旧的整笔拒写语义；新调用同时带
scope/reset token 时，目标 session 未变即可写自己的映射，只有 selection token 仍相同时才
允许移动 current。scope fence 记录最多保留 128 个键；达到上限时自动跨 reset fence，
以失效全部旧 token 换取有界状态。

日志中的 `conversation_id_out` 表示本枪从 Dify 观察到的 CID，不单独证明它已写入
`sessions.json`；fence 拒绝迟到写回时仍可能保留该观测值。持久绑定以 `/sessions` 返回的
`by_cc` / `by_agent` 为准。

resolve 只读；写盘仅 remember / `POST /sessions/new`（解绑）/ `POST /sessions/switch`。
子代理成功收尾写入 `remember_agent`；父 session 解绑时同步清理其子代理映射，`clear_all`
清理全部命名空间。
`binding_epoch` 是当前 `current` 选择的兼容序号；每个 parent session 另有 scope fence，
`clear_all` 另有 reset fence。定向解绑/切换若目标 session 的 scope fence 已变化，拒绝该
session 的迟到写入；若只是 selection epoch 变化而目标 scope 仍有效，则保留目标 `by_cc`
映射，但不移动新的 `current`。`clear_all` 的 reset fence 会拒绝所有旧请求。

表中的「成功后 remember」指**工作流成功收尾**：`error`、`workflow_error`，以及 status ∈
{failed, error, cancelled, canceled, stopped} 一律不写 `sessions.json`——失败枪即便已拿到
conversation_id 也不留痕，否则会把一个跑坏的 conversation 绑成主指针。

## 计费

按次：chat-messages 被上游接受（HTTP <400 开流）即记一枪——上游 LLM 已运行，与
正文有无分开。opus/haiku 单价见 `.env`；这是本地账本，
不是官方账单（token 估算仅供上下文占用感）。

### 不打上游的情形

| 情形 | 出口 | 计费 |
|:---|:---|:---|
| terminal 待决释放 / 指纹回放 | 本地预存答复（仅主窗口枪） | 不计 |
| 子代理动作状态 / 占位模板枪 | 本地生成短句 / 固定说明串 | 不计 |
| `400` 已知 input 表单或持久变量超限 | 拒绝，不裁剪、不发送 | 不计 |
| 非流 single-flight `replay` / `join` | 回放 180 秒内同指纹的成功结果，或接驳在途的同指纹任务 | 不新增（原枪已计） |

前三行是真本地短路——这一枪根本没有对应的上游任务；最后一行有一个上游任务正在别处运行或
已运行过，只是本枪不新增调用，岚确实在为它工作。本表只列 `/v1/messages` 的出口；
非业务端点（见 `README.md`）从不碰上游。

## 落盘

本地存储各有容量边界：`by_cc` 32 条 LRU、子代理映射总计 64 条 · `agents.json` 最多 32 个父
session、每父 32 个 agent · read_cache 每 user 40 条 LRU ·
terminal 待决与指纹回放各 24 小时、各最多 64 项 · 请求日志最近 200 份（按文件名时序淘汰）。
`last_request.json` 恒为最近一份的镜像，供 `/debug/last-request` 与人工排障用固定路径。
`folded_query` 只对 title 枪落盘——只有那一档的出站 query 等于整包折叠，其余枪型可从
同一文件的 `raw_body` 原样重算。

重启只恢复这些 JSON 已提交的事实，不恢复 HTTP client、会话锁或进程内 single-flight 的
在途任务；服务关闭时后者会被取消。状态文件以临时文件替换方式原子写入，缺失、空白、损坏
或顶层类型不符时按默认状态 fail-open，因此“服务能启动”不等于旧状态仍在。

`data/*.json` 是内部持久形状，不是稳定外部 API。人工排障可以读取；读取器容忍额外旧字段，
但不保证继续写出它们。若要让别的程序长期依赖其中字段，须先另立带版本的公开契约；不能让
一次内部精简意外冻结全部存储结构。

## 标记辞典

| 标记 | 谁写 | 作用 |
|:---|:---|:---|
| `[[cc_route:haiku\|opus]]` | 代理 · query 首行 | 岚 if-else 分档 |
| `[[cc_struct:on]]` | 代理 | 路由到结构化出口分支 |
| `[[cc_tool_continue]]` | 代理 | 工具续写壳 |
| `[[cc_tools:on]]` | 代理 · query 尾 | 工具提醒（约束居近） |
| `[[cc_need_read]]` | 代理 | 引用路径但无正文 → 须先 Read |
| `[[cc_images:N\|failed]]` | 代理 | 附图成功 / 失败（勿装看见） |
| `[[cc_read_cache:miss]]` | 代理 | Wasted call 无缓存可重放 |
| `[[cc_agents:pending\|result_ready\|result_carry]]` | 代理 · route 之后 | 后台 Agent 生命周期阶段，各带对应行为约束 |
| `[[lan_agent_transport:…]]` | Claude Code hook → 代理入口 | HMAC 身份载体；验签后剥除，不进 Dify |
| `[[cc_unicode_wire:on]]` / `⟦U+hhhhhh⟧` | 代理 | 非 BMP 线缆已启用 / Unicode 码点的可逆表示；返回前还原 |
| `[[tool_use]]` / `<tool_use>` / `<tool_call>` | 模型 | 文本协议工具块 |
| `[[raw:键]]…[[/raw:键]]` | 模型 | 零转义原文块（以块为准覆盖同名键） |
| `[[after_success]]…[[/after_success]]` | 模型 → 代理暂存 | 仅 Write/Edit 全成功后本地释放的终结答复 |
| `[[cc_tools_json]]…` | 模型 | 结构化外壳围栏形态 |
| `testandlife` | 用户 | 强制 haiku 冒烟 |

## 已知不可为

- thinking `signature` 恒空：Dify 无 Anthropic 可校验签名，官方级多轮思考 UI 不可完整模拟。
- `max_tokens` 等入站采样参数被忽略（上游节点自配）。
- Claude 官方订阅的用量美元与本代理 `/v1/usage` 的按次账本是两条线，勿互相换算。

## 远期备案：消息索引层（未执行）

`body["messages"]` 在一枪之内被完整或近完整遍历十余次，各消费者分别回答同一批问题：
哪条是本轮收尾 user、哪个 `tool_use` 配哪个 `tool_result`、某个 Read 是否完整。
已勘明的遍历点：`_agent_lifecycle_context` 内部三轮 · `parse_payload` 的 system 循环与
reminder 循环 · `_tool_trace_index` · `_extract_tool_blocks_from_messages` 主循环 ·
`_latest_full_file_states` · `build_history_and_current` · `estimate_input_tokens_from_request` ·
`extract_images_from_last_user` · cache 与 terminal 各自的摄取。此外「末条 user」这一个判定
另有五套彼此独立的实现（`parse._trailing_user_index` · `parse.extract_images_from_last_user` ·
`plan._last_user_text` · `log` 的摘要循环 · `terminal` 的批次定位），分布四个模块。

代价不在遍历本身，而在**判断逻辑跟着复制**：路径归一曾有两份实现且在 UNC 上口径分歧
（已收敛到 `parse.normalize_path`，两套键空间当时恰好不相交，故未成事故）。terminal 的批次
内去重另有一套 `ntpath.normcase(normpath(…))`，**不在这次收敛范围内**——它只在单枪批次内
两两比较、键空间不与缓存或「最新完整状态」相交，且比通用归一更强（能识破 `a/../b` 与 `b`
是同一个目标），弱化它会削掉短路的安全性。同类分歧还会出现在任何新增的消费者上。

启用条件不是「看起来重复」，而是出现下列任一：① 又一处需要自行判定 trailing user 或
tool_use↔result 配对的新消费者；② 一次因两处判定口径不一致而产生的实际错误。届时的契约：

- 由 `parse` 在入口一次性产出只读索引（trailing user 下标、calls/results 映射、
  当前 tool_result id 集合、最新完整文件状态、图块位置），随 `parse_payload` 一并交出。
- 下游只读索引，不再触碰原始 `messages`；判定函数各只保留一个定义。
- 索引不缓存跨请求状态——每枪的消息链是完整的，缓存反而会引入陈旧。

这项牵动 `parse` / `cache` / `terminal` / `log` / `answer` 五个模块的入口，且守则
5 / 7 / 11 / 15 / 19 的端点面锚点须先就位（见 `tests/test_precepts.py`）。

## 单变量无损分片

启用条件是 `Tool_invocation`、`Current_Context` 或 `History` 在线缆处理后仍越过表单字符数
或持久变量内存边界。代理侧已经实现；本机私有文件
`C:\Users\杨\Desktop\Key chain\AI 装置\本地代理\岚.yml` 是应用侧部署基线（不进入公开仓库），完整接线为：

| 层 | 配置契约 |
|:---|:---|
| Conversation variables | `Tool_invocation` 声明 `_1…11`，`History` 声明 `_1`，`Current_Context` 声明 `_1…2`；初值均为空字符串 |
| Start | 14 个编号分片均为可选 Paragraph，`max_length=233333` |
| 变量赋值器 | 每枪以 Start 同名值 `over-write` 对应 conversation variable |
| 三个 LLM | 每组按 `基键（第 0 片） → _1 → … → _N` 相邻引用；工具调用至 `_11`，历史至 `_1`，当轮上下文至 `_2`，片间不插入文本 |

Start 与赋值器沿用 `Tool_invocation` 组 → `System_Description` → `History` 组 →
`Current_Context` 组的界面顺序。为延续现有 DSL 的排列，本机私有 `岚.yml` 中
`conversation_variables` 块按这一逻辑的反向存放；这只是文件顺序，不改变分片顺序或语义。

- 未超限时仍使用原基键，并以空串清掉全部编号槽；超限时保留基键作为第 0 片，再沿同名槽连续分片。
- 按 Unicode 字符边界连续切片；线缆 token 不跨槽。各片独立通过其 `max_length` 与
  `sys.getsizeof <= 204800`，query 携带逻辑类型及实际字段顺序。
- Dify 提示按序分别引用各片，不在代码节点重新拼成一个再次受单变量上限约束的值。
- `/parameters` 是可用槽的唯一事实源：草稿未发布、命名不符或槽数不足时仍返回
  `400 invalid_request_error`；绝不改塞其他语义字段、丢尾、摘要替代或静默裁剪。

## 远期备案：原生 Messages（未执行）

本代理成立的主要前提是 Dify 专业计划提供只能经 Dify 使用的模型额度；故现阶段不增加
外部 Messages 上游、密钥或路由。若未来因个性化设计另有真 Anthropic Messages 上游，
再在两种形态中择一：① CC Switch 新建独立 Native 配置直接切换；② lan 以显式 model
alias 分流，工具密集枪走 native、纯聊留岚。不得仅因请求含 `tools[]` 自动漂移上游。

原生通道的价值是 tools 结构原样透传并省去文本协议；费用按该上游规则另计。它本身不会消除
Read→tool_result→Write 的模型依赖轮次。

## 北极星

在 Dify 额度约束内，工具执行留在 CC，lan 负责忠实转译、同层批量、上下文纯化，并以
显式 terminal 契约消除可证明多余的成功确认枪；不以猜测短路换取表面上的少一轮。
